import 'package:flutter/material.dart';

class CadastroFornecedoresPage extends StatelessWidget {
  const CadastroFornecedoresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cadastro de Fornecedores'),
      ),
    );
  }
}