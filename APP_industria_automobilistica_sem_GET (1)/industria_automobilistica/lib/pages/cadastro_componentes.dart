import 'package:flutter/material.dart';

class CadastroComponentesPage extends StatelessWidget {
  const CadastroComponentesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cadastro de Componentes'),
      ),
    );
  }
}